<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <DataTemplate x:Key="ButtonPromptA">
    <Viewbox>
      <Image Source="{ThemeFile 'Images/ButtonPrompts/Xbox/button_a.png'}" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptB">
    <Viewbox>
      <Image Source="{ThemeFile 'Images/ButtonPrompts/Xbox/button_b.png'}" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptX">
    <Viewbox>
      <Image Source="{ThemeFile 'Images/ButtonPrompts/Xbox/button_x.png'}" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptY">
    <Viewbox>
      <Image Source="{ThemeFile 'Images/ButtonPrompts/Xbox/button_y.png'}" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptStart">
    <Viewbox>
      <Image Source="{ThemeFile 'Images/ButtonPrompts/Xbox/button_start.png'}" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptBack">
    <Viewbox>
      <Image Source="{ThemeFile 'Images/ButtonPrompts/Xbox/button_back.png'}" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptLB">
    <Viewbox>
      <TextBlock FontFamily="{DynamicResource FontXboxOneController}"
        Foreground="{DynamicResource TextBrush}" Text="f" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptRB">
    <Viewbox>
      <TextBlock FontFamily="{DynamicResource FontXboxOneController}"
        Foreground="{DynamicResource TextBrush}" Text="z" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptLT">
    <Viewbox>
      <TextBlock FontFamily="{DynamicResource FontXboxOneController}"
        Foreground="{DynamicResource TextBrush}" Text="j" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptRT">
    <Viewbox>
      <TextBlock FontFamily="{DynamicResource FontXboxOneController}"
        Foreground="{DynamicResource TextBrush}" Text="v" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptLS">
    <Viewbox>
      <TextBlock FontFamily="{DynamicResource FontXboxOneController}"
        Foreground="{DynamicResource TextBrush}" Text="g" />
    </Viewbox>
  </DataTemplate>

  <DataTemplate x:Key="ButtonPromptRS">
    <Viewbox>
      <TextBlock FontFamily="{DynamicResource FontXboxOneController}"
        Foreground="{DynamicResource TextBrush}" Text="x" />
    </Viewbox>
  </DataTemplate>

</ResourceDictionary>